# Create .env.dev and .env.prd files from this to store sensitive data only.
# For general data use serverless.environment.yml and serverless.custom.yml files.
MONGODB_URI=mongodb+srv://<user>:<password>@<host>/?retryWrites=true&w=majority
ERROR_NOTIFICATION_RECIPIENT_ADDRESS=example@domain.com # or leave empty to disable
ERROR_NOTIFICATION_RECIPIENT_ADDRESS_COPY=example@domain.com # or leave empty to disable